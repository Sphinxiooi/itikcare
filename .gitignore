venv/
.venv/
__pycache__/
*.py[cod]
*.so

.env

db.sqlite3
db.sqlite3-journal

media/
staticfiles/
# static/dist/output.css is committed on purpose: the Railway build has no Tailwind
# binary (the local one is Windows-only), so it ships the pre-built, minified CSS.
# Rebuild it before deploying any template/CSS change (see README).

tailwindcss.exe

*.joblib
*.pkl
*.log
models/*.json
~$*

data_backup.json
# Farm data export (password hashes, emails) -- never commit; see deploy/RAILWAY.md.
deploy/farm_data.json

.vscode/
.idea/
*.swp

.claude/
